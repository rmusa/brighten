<div id="sign_up_form">

  <div id="sign_title">
    <br><p>Sign up</p>
  </div>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= devise_error_messages! %>

    <div><%= f.text_field :name, :placeholder => "Name"  %></div>

    <div><%= f.email_field :email, autofocus: true, :placeholder => "Email"  %></div>

    <div>
      <%= f.label :profile_picture %>
      <div id="profile_picture_field">
        <%= f.file_field :profile_picture %>
      </div>
    </div>

    <div><%= f.password_field :password, autocomplete: "off", :placeholder => "Password" %></div>

    <div><%= f.label :password_confirmation %><br />
      <%= f.password_field :password_confirmation, autocomplete: "off" %></div>

    <div><br><%= f.submit "Sign up", class: "btn btn-large btn-primary"%></div>
  <% end %>

  <%= render "devise/shared/links" %><br>

</div>

<!-- we need to delete old events -->
<div id="why_signup">
  <p>Brighten allows volunteers to find community service events in Cape Town. Whether you are visiting or residing, find some extra time to brighten the mother city! Brighten's users can vary from organizations to people who want to make a difference, because everyone can organize community service events. After signing up, search for an event by interest, date, or keyword. Create an organization and start hosting events! </p>
</div>

<div id="mandela">
  <footer><br><p class="quote">&ldquo;It always seems impossible until it&rsquo;s done.&rdquo; &ndash; Nelson Mandela</p><br></footer>
</div>

